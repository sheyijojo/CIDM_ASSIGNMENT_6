from distutils.core import run_setup
from setuptools import find_packages, setup

#settting this directory up as a package

setup(
    name= "flaskner",
    version="0.0.1",
    description = "A Simple NER API")